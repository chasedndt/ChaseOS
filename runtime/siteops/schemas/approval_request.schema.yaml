{"object": "ApprovalRequest", "required": ["approval_id", "tenant_id", "workspace_id", "user_id", "run_id", "workflow_id", "action", "risk_level", "approval_reason", "requested_by", "required_approver_role", "status"], "optional": ["decided_by", "decided_at", "metadata"], "metadata_usage": "optional scoped provenance binding for the approved action target; Browser Skill candidate promotion approvals bind candidate_id, proposed_skill_id, trusted_skill_path, siteops_skill_card_path, source_path, and promotion_action", "status_values": ["pending", "approved", "rejected", "expired"], "scope_required": ["tenant_id", "workspace_id", "user_id"], "secret_values_allowed": false}
