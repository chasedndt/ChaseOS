# VentureOps Workflow Pack Examples

These files are portable workflow-pack examples validated against the VentureOps workflow-pack schema shape. They are not AOR executable manifests and do not prove a workflow has run.

Runnable AOR manifests remain separate files in `runtime/workflows/registry/`.
